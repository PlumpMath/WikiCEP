@model IEnumerable<WikiCEP_Project.Models.TutorialesYouTube>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LinkYouTube)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LinkYouTube)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDTutorial }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDTutorial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDTutorial })
        </td>
    </tr>
}

</table>
